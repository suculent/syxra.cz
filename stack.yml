version: "3"

services:

  web:
    image: suculent/syxracz
    ports:
      - 80:30001
    labels:
      - "traefik.frontend.entryPoints=http"
      - "traefik.enable=true"
      - "traefik.backend=syxra-cz"
      - "traefik.port=30001"
      - "traefik.docker.network=traefik-public"
      - "traefik.frontend.rule=Host:syxra.cz"
